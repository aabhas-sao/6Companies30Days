#include <bits/stdc++.h>
using namespace std;

#define ll long long int
#define MOD (int)(1e9 + 7)

int soln(string s) {
  int one = 1, two, n = s.size();
  for (int i = n - 1; i >= 0; i--) {
    if (i < n - 1 && ((s[i] == '1' || s[i] == '2') && s[i + 1], '7')) {
      
    }
  }

  return
}

int main() {
#ifndef ONLINE_JUDGE
  freopen("input.txt", "r", stdin);
  freopen("output.txt", "w", stdout);
#endif
  string s;
  cin >> s;
  cout << soln(s) << endl;

  return 0;
}